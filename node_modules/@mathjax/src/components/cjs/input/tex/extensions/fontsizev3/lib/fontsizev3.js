const {combineWithMathJax} = require('../../../../../../../cjs/components/global.js')
const {VERSION} = require('../../../../../../../cjs/components/version.js');

const module1 = require('../../../../../../../cjs/input/tex/fontsizev3/FontSizeV3Configuration.js');

if (MathJax.loader) {
  MathJax.loader.checkVersion('[tex]/fontsizev3', VERSION, 'tex-extension');
}

combineWithMathJax({_: {
  input: {
    tex: {
      fontsizev3: {
        FontSizeV3Configuration: module1
      }
    }
  }
}});
